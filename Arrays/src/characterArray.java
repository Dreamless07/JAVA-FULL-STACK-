public class characterArray {
    public static void main(String[] args) {
        char name[] = {'S', 'A', 'A', 'D'};
        for (int i = 0; i < name.length; i++) {//with for loop increment till the last index of array
            System.out.println(name[i]);
        }
    }
}
